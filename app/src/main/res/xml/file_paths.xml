<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Example: Share files from the app's internal cache directory -->
    <cache-path name="my_cache_files" path="." />

    <!-- Example: Share files from the app's internal files directory -->
    <files-path name="my_files" path="images/" />

    <!-- Example: Share files from external cache directory -->
    <external-cache-path name="my_external_cache_files" path="." />

    <!-- Example: Share files from the root of the external storage (use with caution) -->
    <!-- <external-path name="external_files" path="." /> -->

    <!-- Add other paths as needed -->
</paths>